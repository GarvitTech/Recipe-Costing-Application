<Page x:Class="RecipeCostingApp.Views.BulkCostingPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Bulk Costing">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Label Grid.Row="0" Content="📊 Bulk Recipe Costing" Style="{StaticResource SectionHeader}" FontSize="24"/>

        <Border Grid.Row="1" Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource BorderBrush}" 
                BorderThickness="1" CornerRadius="5" Padding="20" Margin="0,0,0,20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="300"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="150"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Label Grid.Column="0" Content="Recipe:" Style="{StaticResource FieldLabel}"/>
                <ComboBox Grid.Column="1" Name="CmbBulkRecipes" Style="{StaticResource InputComboBox}" 
                         DisplayMemberPath="Name" SelectionChanged="CmbBulkRecipes_SelectionChanged"/>

                <Label Grid.Column="2" Content="Multiplier:" Style="{StaticResource FieldLabel}" Margin="20,0,10,0"/>
                <TextBox Grid.Column="3" Name="TxtMultiplier" Style="{StaticResource InputTextBox}" 
                        Text="50" TextChanged="TxtMultiplier_TextChanged"/>

                <StackPanel Grid.Column="4" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Name="BtnCalculateBulk" Content="🧮 Calculate" Style="{StaticResource PrimaryButton}" 
                           Margin="0,0,10,0" Click="BtnCalculateBulk_Click"/>
                    <Button Name="BtnGenerateBulkPdf" Content="📄 Generate PDF" Style="{StaticResource PrimaryButton}" 
                           Click="BtnGenerateBulkPdf_Click"/>
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource BorderBrush}" 
                    BorderThickness="1" CornerRadius="5" Margin="0,0,10,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Label Grid.Row="0" Content="Bulk Ingredient Requirements" Style="{StaticResource SectionHeader}" Margin="15,15,15,0"/>
                    
                    <DataGrid Grid.Row="1" Name="DgBulkIngredients" Style="{StaticResource ModernDataGrid}" Margin="15">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Ingredient" Binding="{Binding Ingredient.Name}" Width="200"/>
                            <DataGridTextColumn Header="Unit Qty" Binding="{Binding Quantity}" Width="80"/>
                            <DataGridTextColumn Header="Bulk Qty" Binding="{Binding BulkQuantity}" Width="100"/>
                            <DataGridTextColumn Header="Unit" Binding="{Binding Ingredient.Unit}" Width="60"/>
                            <DataGridTextColumn Header="Total Cost" Binding="{Binding BulkCost, StringFormat=C2}" Width="100"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>

            <Border Grid.Column="1" Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource BorderBrush}" 
                    BorderThickness="1" CornerRadius="5">
                <StackPanel Margin="15">
                    <Label Content="Bulk Cost Summary" Style="{StaticResource SectionHeader}"/>
                    
                    <Border Background="{StaticResource CalculatedBrush}" Padding="15" CornerRadius="5">
                        <StackPanel>
                            <TextBlock Name="TxtBulkMultiplier" Text="Multiplier: x0" FontSize="14" FontWeight="Bold" Margin="0,5"/>
                            <TextBlock Name="TxtBulkRecipeCost" Text="Unit Recipe Cost: $0.00" FontSize="14" Margin="0,5"/>
                            <TextBlock Name="TxtBulkTotalCost" Text="Total Recipe Cost: $0.00" FontSize="16" FontWeight="Bold" Margin="0,10"/>
                            <Separator Margin="0,10"/>
                            <TextBlock Name="TxtBulkFinalCost" Text="Final Cost: $0.00" FontSize="16" FontWeight="Bold" Margin="0,5"/>
                        </StackPanel>
                    </Border>
                    
                    <Label Content="Quick Multipliers" Style="{StaticResource FieldLabel}" Margin="0,20,0,10"/>
                    <StackPanel>
                        <Button Content="x25" Style="{StaticResource PrimaryButton}" Margin="0,2" Click="BtnQuickMultiplier_Click" Tag="25"/>
                        <Button Content="x50" Style="{StaticResource PrimaryButton}" Margin="0,2" Click="BtnQuickMultiplier_Click" Tag="50"/>
                        <Button Content="x100" Style="{StaticResource PrimaryButton}" Margin="0,2" Click="BtnQuickMultiplier_Click" Tag="100"/>
                        <Button Content="x200" Style="{StaticResource PrimaryButton}" Margin="0,2" Click="BtnQuickMultiplier_Click" Tag="200"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Page>